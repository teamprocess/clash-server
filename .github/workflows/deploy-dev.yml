name: Docker Compose CI/CD (Develop)

on:
  pull_request:
    branches: [ "develop" ]
    types: [ closed ]

jobs:
  deploy:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build with Gradle
        run: ./gradlew clean build -x test

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build and Push Docker Image
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            -t ${{ secrets.DEVELOP_DOCKER_IMAGE }}:${IMAGE_TAG} \
            --push \
            .

      - name: Deploy to Dev Server (only clash-db, clash-server)
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DEVELOP_SERVER_IP }}
          username: ${{ secrets.DEVELOP_SSH_USER }}
          key: ${{ secrets.DEVELOP_SSH_PRIVATE_KEY }}
          port: ${{ secrets.DEVELOP_SSH_PORT }}
          script_stop: true
          script: |
            set -eu
            cd ~/app

            echo "DOCKER_IMAGE=${{ secrets.DEVELOP_DOCKER_IMAGE }}" > .deploy.env
            echo "IMAGE_TAG=${{ github.sha }}" >> .deploy.env

            export $(cat .deploy.env | xargs)

            docker compose pull clash-db clash-server
            docker compose up -d --no-deps clash-db clash-server
