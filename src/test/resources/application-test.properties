# 1. DB ?? (TINYINT ??? ?? MODE=PostgreSQL ??)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.data.redis.password=

# 2. JPA/Hibernate ?? (H2 ?? ??)
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.defer-datasource-initialization=false
spring.flyway.enabled=false
realtime.socketio.port=0
ENVIRONMENT=dev

# 3. ??? ?? ??? (???? ?? ??)
spring.cloud.gateway.server.webflux.enabled=false
spring.sql.init.mode=never
spring.sql.init.encoding=UTF-8

# 4. Google reCAPTCHA (test values)
google.recaptcha.key.site=test-site-key
google.recaptcha.key.secret=test-secret-key
google.recaptcha.key.url=https://www.google.com/recaptcha/api/siteverify
